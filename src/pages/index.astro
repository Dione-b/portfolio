---
import Layout from "../layouts/Layout.astro";
import HorizontalScroll from "../components/HorizontalScroll.astro";
import Hero from "../components/Hero.astro";
import FeaturedProjects from "../components/FeaturedProjects.astro";
import ProjectsTable from "../components/ProjectsTable.astro";
import Articles from "../components/Articles.astro";
import Skills from "../components/Skills.astro";
import Timeline from "../components/Timeline.astro";
import Contact from "../components/Contact.astro";
---

<Layout>
  <HorizontalScroll labels={["Início", "Projetos", "Tabela", "Artigos", "Skills", "Timeline", "Contato"]}>
    <div class="w-screen h-screen flex-shrink-0 overflow-y-auto flex flex-col items-center justify-start md:justify-center py-12 md:py-0" data-scrollable>
      <Hero />
    </div>
    <div class="w-screen h-screen flex-shrink-0 overflow-y-auto" data-scrollable>
      <FeaturedProjects />
    </div>
    <div class="w-screen h-screen flex-shrink-0 overflow-y-auto flex flex-col items-center justify-start md:justify-center py-12 md:py-0" data-scrollable>
      <ProjectsTable />
    </div>
    <div class="w-screen h-screen flex-shrink-0 overflow-y-auto flex flex-col items-center justify-start md:justify-center py-12 md:py-0" data-scrollable>
      <Articles />
    </div>
    <div class="w-screen h-screen flex-shrink-0 overflow-y-auto flex flex-col items-center justify-start md:justify-center py-12 md:py-0" data-scrollable>
      <Skills />
    </div>
    <div class="w-screen h-screen flex-shrink-0 overflow-y-auto" data-scrollable>
      <Timeline />
    </div>
    <div class="w-screen h-screen flex-shrink-0 overflow-y-auto flex flex-col items-center justify-start md:justify-center py-12 md:py-0" data-scrollable>
      <Contact />
    </div>
  </HorizontalScroll>
</Layout>
